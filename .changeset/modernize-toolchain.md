---
"@craf-te/filmer": patch
---

Modernize the build toolchain: migrate linting/formatting from ESLint + Prettier to Biome, switch the package manager to npm, and upgrade TypeScript, Vite, and Vitest to their latest majors. No runtime or public API changes — this is a dev-tooling and supply-chain security update (dependency audit: 0 vulnerabilities). The published package is now released with npm provenance.
